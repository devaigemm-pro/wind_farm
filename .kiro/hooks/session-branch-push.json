{
  "version": "v1",
  "hooks": [
    {
      "name": "Session cleanup (commit local, no push)",
      "trigger": "Stop",
      "description": "At session end: kills preview, commits locally (no push). Push only happens on user-approved merge to main. This prevents Vercel auto-deploy of session branches.",
      "action": {
        "type": "command",
        "command": "zsh /Users/Apple/Documents/Kiro/wind_farm/scripts/session-branch-push.sh",
        "timeout": 60
      }
    }
  ]
}
