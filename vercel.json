{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == session/* ]]; then echo 'Skipping session branch'; exit 0; fi; exit 1"
}
